@model IEnumerable<ORCA.Models.ConsultantProfile>

@{
    ViewBag.Title = "CPListView";
}

<h2>CPListView</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.TitleDegree)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.KeyWordList)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.TitleDegree)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.KeyWordList)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { /* id=item.PrimaryKey */ }) |
            @Html.ActionLink("Details", "Details", new { /* id=item.PrimaryKey */ }) |
            @Html.ActionLink("Delete", "Delete", new { /* id=item.PrimaryKey */ })
        </td>
    </tr>
}

</table>
